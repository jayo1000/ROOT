<%@ page contentType="text/html; charset=utf-8" %>
<%
/* HttpSession sessions = request.getSession();
if(sessions != null){
	for(HttpSession session : sessions){
		if(session.getName().equals("loginId")){
			session.setMaxAge(0);
			session.setPath("/");
			response.addCookie(session);
			response.sendRedirect("/");
		}
	}	
} */
%>